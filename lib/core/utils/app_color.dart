import 'package:flutter/material.dart';

abstract class AppColor {
  // Define your color palette here
  // static const Color primary = Color(0xD0432BDC);
  static const Color background = Color.fromARGB(203, 235, 243, 246);
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF757575);
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF388E3C);
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  static const Color primary = Color(0xFF19A580);
  static const Color primaryDark = Color(0xFF106653);

  // Sidebar
  static const Color sidebar = Color(0xFF19A580);
  static const Color sidebarDark = Color(0xFF106653);

  // Background
  static const Color lightBg = Color(0xFFFFFFFF);
  static const Color darkBg = Color(0xFF181818);

  // Text
  static const Color lightText = Color(0xFF333333);
  static const Color darkText = Color(0xFFF0F0F0); // تم تصحيحه من FOFOFO

  // Status Colors
  static const Color green = Color(0xFF10B981);
  static const Color yellow = Color(0xFFFFCE56);

  // Borders
  static const Color lightBorder = Color(0xFFE5E7EB);
  static const Color darkBorder = Color(0xFF3D3D3D);
  
  // Inputs
  static const Color lightInput = Color(0xFFF9FAFB);
  static const Color darkInput = Color(0xFF2D2D2D);
}


